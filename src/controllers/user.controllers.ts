class UserController {
  register = async () => {
    return { status: "OK" };
  };

  login = async () => {
    return { status: "OK" };
  };

  logout = async () => {
    return { status: "OK" };
  };

  profile = async () => {
    return { status: "OK" };
  };
}

export default UserController;
